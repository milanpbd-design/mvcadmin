# Server Configuration
API_PORT=5000
NODE_ENV=production

# Build Configuration
BUILD_PATH=.dist

# Database (Optional - currently using file-based storage)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/myvetcorner?retryWrites=true&w=majority

# Security (IMPORTANT: Change these for production!)
JWT_SECRET=your-secure-random-jwt-secret-key-here-change-this
ADMIN_PASSWORD=your-secure-admin-password-change-this
ADMIN_USERNAME=admin

# Features
NO_MONGO=true

# IMPORTANT FOR HOSTINGER DEPLOYMENT:
# 1. Create a .env file in your project root with these variables
# 2. NEVER commit .env to git (it's in .gitignore)
# 3. Set strong values for JWT_SECRET and ADMIN_PASSWORD
# 4. Upload .env file separately via FTP/File Manager to your Hostinger server
