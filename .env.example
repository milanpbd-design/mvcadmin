# Build Configuration
BUILD_PATH=.dist

# Admin Password (used for client-side authentication)
# IMPORTANT: Change this for production!
ADMIN_PASSWORD=Master@47

# GitHub Configuration (for publishing via GitHub Actions)
REACT_APP_GITHUB_TOKEN=your-github-personal-access-token
REACT_APP_GITHUB_OWNER=your-github-username
REACT_APP_GITHUB_REPO=your-repo-name

# IMPORTANT NOTES:
# 1. Create a .env file in your project root with these variables
# 2. NEVER commit .env to git (it's in .gitignore)
# 3. Set a strong ADMIN_PASSWORD for your admin panel
# 4. Admin changes are stored in localStorage and need to be exported/committed to persist
